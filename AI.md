## Week 4

**Task:** Updating Javadoc for refactored `Kiki.java` and newly added files

**Tool used:** Gemini

**Goal:** To update method documentation (Javadocs) to reflect the architectural change in `Kiki.java` and to add Javadoc for newly added files including `DialogBox.java`, `Launcher.java`, `Main.java`, and `MainWindow.java`.

**What worked:**
* The AI correctly generated new Javadoc for new files and the refactored methods in Kiki.java

**What didn't work:**
* N/A (The tool completed the task without errors).

**Time saved:** ~1 hour

---

## Week 5

**Task:** Updating Javadoc for `Kiki.java` after fixing the long method

**Tool used:** Gemini

**Goal:** To generate Javadoc for the newly extracted methods (e.g., `addTodo`, `addDeadline`) resulting from refactoring the long `getResponse` method in `Kiki.java`.

**What worked:**
* The AI correctly generated Javadoc comments for the new methods in `Kiki.java`.

**What didn't work:**
* N/A (The tool completed the task without errors).

**Time saved:** ~30 minutes